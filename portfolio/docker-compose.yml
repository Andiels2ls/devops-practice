version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: portfolio-web
    ports:
      - "8080:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
      - ./logs:/var/log/nginx
    restart: unless-stopped
    environment:
      - NGINX_HOST=portfolio.local
